JAVA FILES AND THERE PURPOSE:
           
    DSM.java
        Thread Purpose: if message exist, store it locally.
                        dsm uses uses broadcastagent to broadcastSystem 

    LocalMemory.java
        Purpose: store(variable, value) and load(variable)
    BroadcastAgent.java
        Thread Purpose: check if there a broadcast recieved it
    BroadcastSystem.java
        Thread Purpose: checking for a broadcast, and then sends it if there is
    Token.java
        Purpose: has a message - int 
            1. has a different int because multiple rings  
    TokenRingAgent.java
        Thread Purpose
    TokenRing:
        Thread purpose: checking to see a token.

    Processes:
        Thread purpose: passes the token around.

    

